# Review

Written by a fresh session that compares the code with `docs/prd.md`, `docs/adr.md` and `docs/plan.md`. Findings only; the fixes are a separate step.

| Finding | Type (must-have / decision / control) | Fixed before demo? |
|---------|---------------------------------------|--------------------|
| | | |
